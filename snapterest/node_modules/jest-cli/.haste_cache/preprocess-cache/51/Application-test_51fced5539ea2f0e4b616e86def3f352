'use strict';jest.dontMock('../Application.react');

describe('Application', function () {
    var React = require('react');
    var ReactDOM = require('react-dom');
    var TestUtils = require('react-addons-test-utils');
    var Application = require('../Application.react');

    it('gets initial state', function () {
        var application = TestUtils.renderIntoDocument(
        React.createElement(Application, null));

        console.log("application.state: ", application.state);
        console.log("application: ", application);
        expect(application.state.collectionTweets).toBe({});});});